SimpleCache
	side effects possibe?

Cache invalidation
	by time
		bigger interval, if maxAge is bigger

Ignore flag - instead of commenting out memo call

Do not cache errors (option)

test framework nutzen?
SimpleCache auf dem client nutzbar machen, spätestens dann test frmwrk nutzen
